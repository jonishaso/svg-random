import React from 'react'
import random from '../utility'

const Rect = ({ positionX, positionY, w, h }) => (
  <rect x={positionX} width={positionY} width={w} height={h} style={random()}/>
)

export default Rect